# =============================================================================
# DarijaLingo - Terraform Variables
# Copy to terraform.tfvars and fill in values. NEVER commit terraform.tfvars.
# =============================================================================

aws_region        = "us-east-1"
project_name      = "darijalingo"
environment       = "production"
db_instance_class = "db.t3.micro"

# Required — set via tfvars or environment variables:
#   export TF_VAR_db_password="your-secure-database-password"
#   export TF_VAR_jwt_secret_key="your-jwt-secret-key"
#   export TF_VAR_anthropic_api_key="your-anthropic-api-key"

# frontend_url = "https://darijalingo.netlify.app"

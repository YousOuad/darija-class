# Database (local dev)
DATABASE_URL=postgresql+asyncpg://darija:darija_pass@localhost:5432/darija_db
POSTGRES_USER=darija
POSTGRES_PASSWORD=darija_pass
POSTGRES_DB=darija_db

# JWT
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Claude AI
ANTHROPIC_API_KEY=your-anthropic-api-key

# CORS (local dev)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend (local dev — not set on Netlify, use VITE_API_URL env var instead)
VITE_API_URL=http://localhost:8000/api
